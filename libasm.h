

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

size_t      ft_strlen(char const *str);
ssize_t     ft_write(int fd, void const *buff, size_t len);
char        *ft_strcpy(char *dest, const char *src);
